/// <reference types="vite/client" />
// `import.meta.env.BASE_URL` — the build's URL prefix, read by
// `api/client.ts:basePath`. The tsconfig pins `types` to the test globals,
// which is why this reference is explicit rather than implied.
