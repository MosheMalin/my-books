opencv-python-headless>=4.8
# Product uploads (app/adapters/disk_blobs.py): decode-to-validate, EXIF
# normalisation and thumbnails. Already present transitively via scikit-image,
# declared because relying on somebody else's transitive dep is how an install
# breaks on the machine that matters.
pillow>=10.0
# stdlib urllib is used for NLI HTTP; no extra dep needed there.
pytesseract>=0.3.10
rapidfuzz>=3.0
scipy>=1.10
scikit-image>=0.21
# web UI (booksnap.server); the core pipeline does not import these.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
# tests only: fastapi.testclient needs it. No pytest — tests/run_all.py is the
# runner (see CLAUDE.md "Tests").
httpx>=0.27
